// vitest のサンプル
// vitest がテストケースを一つも見つけられないエラーになるので、そのエラーを回避するためにも書いている。
// TODO(vitest-example): vitest のサンプルが必要ならこのファイルを削除する
describe('example', () => {
  it('should return 1', () => {
    expect(1).toBe(1);
  });
})
